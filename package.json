{
  "name": "tidelight",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:client\" \"npm:dev:host\"",
    "dev:client": "vite",
    "dev:host": "node server/index.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host --port 4173"
  },
  "dependencies": {
    "three": "^0.170.0",
    "trystero": "^0.25.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
